# Self-Defense Strategy Toolkit Requirements
# Core dependencies (minimal - uses mostly stdlib)

numpy>=1.20.0          # For acoustics physics calculations
matplotlib>=3.4.0      # Optional: for visualizing acoustics model

# All other functionality uses Python standard library:
# - hashlib (cryptographic hashing)
# - json (data serialization)
# - subprocess (system commands)
# - pathlib (file handling)
# - datetime (timestamps)
# - dataclasses (data structures)
# - plistlib (macOS plist parsing)

# Optional dependencies for enhanced functionality:
# psutil>=5.8.0        # Alternative process/network monitoring
# cryptography>=3.4.0  # Enhanced cryptographic operations
